# التقرير النهائي لتدقيق وتنظيف مشروع Moudie NetPlay

**المعد:** Manus AI

**تاريخ التدقيق:** 6 سبتمبر 2026

**نطاق التدقيق:** نسخة GitHub الحالية مع دمج ملفات العمل الأخيرة الخاصة بسياسة سعة الغرف وNetPlay، ثم تشغيل الفحوصات والتنظيف في نسخة تدقيق مستقلة.

## 1. الخلاصة التنفيذية

المشروع عبارة عن تطبيق Expo/React Native متعدد المنصات مع خادم Express وtRPC وقاعدة بيانات MySQL عبر Drizzle، إضافة إلى قنوات Socket.IO للمزامنة واللعب الشبكي، ومكوّن Android أصلي يضم نوى Libretro لمحاكيات Famicom/NES وPlayStation 1. البنية الأساسية قابلة للبناء والتشغيل من ناحية TypeScript واختبارات الوحدة، وتحتوي على فصل واضح نسبيًا بين واجهة الغرف، طبقة API، طبقة NetPlay، والعقد المشتركة.

أظهر التدقيق أن آخر تغييرات السعة كانت صحيحة في معظم الطبقات، لكنها ترافقت مع انحدار وظيفي مهم في `server/routers.ts`: أزيلت إجراءات الردهة العامة `publicList` و`joinPublic`، وأزيل تحديد معدل عمليات الغرف. تمت استعادة الإجرائين وتطبيق سياسة السعة عليهما، كما تم إصلاح مسار NetPlay حتى لا يثق في رقم المقعد المرسل من العميل ولا يسمح بانتحال مقعد لاعب آخر.

تم تنظيف خمس صور تجريبية غير مستخدمة من قالب React، وإزالة مخرجات Android المترجمة التي كانت متتبعة داخل Git، وإزالة `android/local.properties` الذي يحتوي على مسار SDK محلي خاص ببيئة واحدة. تمت إضافة المسار الأخير إلى `.gitignore` حتى لا يعود إلى المستودع.

الحالة النهائية جيدة من ناحية الاختبارات المصدرية، لكنها ليست جاهزة أمنيًا للإطلاق العام قبل معالجة ثلاث نقاط رئيسية: جعل حجز مقاعد الغرف ذريًا داخل معاملة قاعدة البيانات، تقييد CORS إلى قائمة أصول معروفة، ومعالجة تقرير اعتماديات npm الذي أظهر 147 ثغرة، منها 80 عالية و3 حرجة. معظم تقرير الاعتماديات عابر وموجود في أدوات Expo وسلسلة البناء، لذلك يلزم تصنيف الاستخدام الفعلي قبل التحديث الشامل.

## 2. وصف البنية الحالية

| الطبقة | التنفيذ | التقييم |
|---|---|---|
| تطبيق الهاتف والويب | Expo 54، React Native 0.81، Expo Router | منظم وقابل للفحص النوعي |
| API | Express + tRPC | واضح، مع تحقق Zod في نقاط الغرف |
| قاعدة البيانات | MySQL + Drizzle ORM | مخطط مناسب مبدئيًا، لكنه يفتقد قيودًا علائقية وسلوكًا ذريًا للحجز |
| اللعب الشبكي | Socket.IO، خادم عام وخادم Universal منفصل | يدعم المصادقة والمزامنة، ويحتاج تقوية CORS والحدود التشغيلية |
| الصوت | LiveKit JWT لكل غرفة | يعزل اسم الغرفة ويصدر رمزًا قصير العمر |
| المحاكيات الأصلية | Kotlin/Android + Libretro arm64 | المسار الأصلي موجود، لكنه يتطلب بيئة Android كاملة للبناء |
| الاختبارات | Vitest | تغطية جيدة للعقود الحرجة، مع اعتماد بعض الاختبارات على قراءة النصوص بدل اختبار السلوك مباشرة |

## 3. التحديثات الأخيرة التي تم تثبيتها

### 3.1 سياسة سعة الغرف

أصبح الحد الموحد ثمانية أعضاء لكل محاكي، مع فصل مقاعد اللعب عن المشاهدة.

| المحاكي | اللاعبون النشطون | المشاهدون | الإجمالي |
|---|---:|---:|---:|
| PlayStation 1 | 4 | 4 | 8 |
| PSP | 4 | 4 | 8 |
| Sega Genesis | 4 | 4 | 8 |
| Arcade | 4 | 4 | 8 |
| Famicom / NES | 2 | 6 | 8 |

المصدر المركزي لهذه السياسة هو `shared/room-capacity.ts`. يعتمد الخادم والواجهات والاختبارات على هذا المصدر بدل تكرار الأرقام في عدة مواضع.

### 3.2 حجز المقاعد والتحقق من البداية

يمنع الخادم بدء الجلسة ما لم يتحقق الحد الأدنى من اللاعبين النشطين، وتكون ملفات اللعبة وبنية المحرك متطابقة، وتكون حالة اللاعبين جاهزة. تم تحديث حاجز بداية الجلسة ليرفض اللاعب الخامس، وتُظهر الواجهات أربعة مقاعد كحد أقصى للمحاكيات القياسية ومقعدين لـNES.

### 3.3 حماية مقاعد NetPlay

كانت دالة `normalizeNetplayInput` تدعم المقعد الموثق، لكن خادم Socket.IO لم يكن يمرر المقعد المحسوب إلى الدالة. أدى ذلك إلى ترك العميل قادرًا على محاولة إرسال رقم مقعد مختلف. تمت إضافة المقعد المحسوب إلى جلسة Socket.IO، وتمريره إلى التطبيع، مع رفض رقم المقعد غير الصالح عندما لا يوجد مقعد موثق.

هذا الإصلاح مهم لأن بيانات الإدخال الشبكي يجب ألا تحدد هوية اللاعب اعتمادًا على قيمة يرسلها العميل. الخادم يحسب المقعد من عضوية الغرفة وترتيب اللاعبين.

### 3.4 استعادة الردهة العامة

تمت استعادة الإجرائين التاليين في `server/routers.ts`:

- `rooms.publicList` لعرض الغرف العامة القابلة للانضمام.
- `rooms.joinPublic` للانضمام إلى غرفة عامة مع التحقق من `visibility`.

تم أيضًا استعادة تحديد معدل عمليات إنشاء الغرف والانضمام إليها لكل عنوان IP ضمن نافذة زمنية، وتم تعديل `publicList` حتى يستخدم قيمة `limit` التي يرسلها العميل بدل تجاهلها واستخدام رقم ثابت.

## 4. نتائج التحليل الوظيفي

### نقاط القوة

يوجد تحقق Zod جيد نسبيًا لأسماء الغرف والأكواد والأدوار ومعرّفات الأعضاء. لا يتم إرسال ملفات ROM إلى الخادم بحسب التصميم؛ الذي ينتقل هو بصمة الملف ومعلومات إصدار المحرك. تُخزَّن رموز العضوية في قاعدة البيانات على شكل SHA-256، بينما يُحفظ الرمز الخام في التخزين الآمن على الجهاز غير الويب. توجد حدود لحجم رسائل Socket.IO وحجم لقطات الحالة، كما توجد حماية من إعادة تطبيق لقطات ذات `syncId` قديم.

فصل خادم الغرف عن خادم Universal NetPlay يقلل التشابك بين واجهة الغرفة وقناة اللعب. كما أن خادم Universal يحسب اللاعب من ترتيب أعضاء الغرفة بدل قبول رقم لاعب مرسل من العميل، وهو نمط صحيح يجب الحفاظ عليه في كل البروتوكولات.

### نقاط الضعف الوظيفية

تُجرى عملية الانضمام حاليًا على شكل قراءة عدد الأعضاء، ثم اتخاذ قرار، ثم إدراج عضو جديد. إذا وصلت طلبات متزامنة في اللحظة نفسها، فقد ترى كل الطلبات سعة متاحة ثم تُدرج أعضاء يتجاوزون الحد. هذا خطر سلامة بيانات، وليس مجرد مشكلة عرض. المعالجة الصحيحة هي معاملة قاعدة بيانات مع قفل صف الغرفة أو إجراء ذري في قاعدة البيانات، مع اختبار تنافسي.

يعتمد تعيين مقاعد NetPlay في بعض المسارات على ترتيب `memberId`. هذا مستقر داخل قاعدة البيانات، لكنه لا يضمن إعادة توزيع مريحة عند خروج لاعب أو تغير العضوية. يجب تثبيت المقعد في سجل العضو أو إنشاء حجز مقعد صريح إذا كان استمرار المقعد بين إعادة الاتصال مطلوبًا.

الاختبارات الحالية قوية في العقود الأساسية، لكنها تستخدم في بعض المواضع `readFileSync` للتحقق من وجود نصوص داخل الملفات. هذه الاختبارات تلتقط حذف الإجراء، لكنها لا تختبر تنفيذ tRPC أو سلوك قاعدة البيانات الحقيقي. يوصى بإضافة اختبارات تكامل باستخدام قاعدة MySQL اختبارية أو طبقة repository قابلة للحقن.

## 5. نتائج التحليل الأمني

### 5.1 نتائج جيدة

يتم إنشاء رموز العضوية باستخدام `randomBytes(32)`، ثم تخزين تجزئة الرمز فقط. يتم التحقق من أن العضو ينتمي إلى الغرفة قبل عرض snapshot أو إصدار رمز LiveKit. يتم تحديد طول النصوص وأحجام snapshots، ويتم رفض أعضاء المشاهدة من أحداث الإدخال في خادم NetPlay.

### 5.2 مخاطر يجب معالجتها

| الأولوية | المشكلة | الموضع | الأثر المحتمل | المعالجة المقترحة |
|---|---|---|---|---|
| عالية | فحص السعة ثم الإدراج غير ذري | `server/routers.ts`, `server/db.ts` | تجاوز حد 8 أعضاء تحت الطلبات المتزامنة | معاملة وقفل صف الغرفة أو عملية إدراج ذرية مع قيود قاعدة البيانات |
| عالية | CORS يعكس أي `Origin` ويستخدم credentials | `server/_core/index.ts`، وإعدادات Socket.IO | توسيع سطح هجمات المتصفح مع رموز الجلسة | قائمة `ALLOWED_ORIGINS` صريحة، ورفض الأصل غير المعروف، مع `Vary: Origin` |
| عالية | 147 ثغرة في شجرة الاعتماديات | `pnpm audit --audit-level=high` | مخاطر سلسلة توريد، مع 80 عالية و3 حرجة | تشغيل `pnpm why`، تحديث مباشر ومدروس، ثم إعادة الاختبارات والبناء |
| متوسطة | rate limiter في الذاكرة فقط | `server/routers.ts` | لا يعمل بين نسخ الخادم، ويمكن فقدانه عند إعادة التشغيل | Redis أو مخزن مشترك في النشر متعدد النسخ |
| متوسطة | `express.json` يسمح بحمولة 50MB | `server/_core/index.ts` | استهلاك ذاكرة أو رفض خدمة على مسارات API غير المحتاجة | تقليل الحد العام واستخدام حدود خاصة للمسارات التي تحتاج حمولة كبيرة |
| متوسطة | المقارنة النصية للرمز المضيف | `server/routers.ts` | ليست مقارنة ثابتة الزمن | استخدام مقارنة ثابتة الزمن بعد توحيد طول القيم، أو الاعتماد على lookup بالتجزئة |
| منخفضة | صلاحية رمز LiveKit ساعتان مع نشر الصوت للجميع | `server/livekit.ts` | نافذة أطول من اللازم لتسريب رمز الصوت | تقليل TTL، وإلغاء الرمز أو تدويره عند مغادرة العضو، وتقرير سياسة المشاهدة/النشر |

تنبيه مهم: تقرير `pnpm audit` يخلط بين اعتماديات تشغيلية واعتماديات أدوات البناء. لا يجوز تنفيذ `pnpm audit --fix` عشوائيًا، لأن تحديث Expo وReact Native قد يسبب تغييرات أصلية كبيرة. يجب أولًا تحديد المسارات التي تصل إلى خادم الإنتاج.

## 6. تحليل API وقاعدة البيانات

مخطط `game_rooms` يفرض نوع النظام والحالة والرؤية، ويفهرس حالة الغرفة و`visibility/status`. مخطط `room_members` يفرض تفرد تجزئة رمز العضوية ويفهرس `roomId`. هذه بداية جيدة، لكن لا يوجد مفتاح أجنبي ظاهر بين `room_members.roomId` و`game_rooms.id`. يؤدي ذلك إلى احتمال بقاء أعضاء يتامى إذا حُذفت غرفة مباشرة من قاعدة البيانات.

لا توجد قيود قاعدة بيانات تضمن أن عدد أعضاء الغرفة لا يتجاوز سعتها، لأن السعة موزعة بين `game_rooms.maxPlayers` وحساب المشاهدين في الخادم. يفضل إضافة مسار حذف/إغلاق رسمي للغرف، ومهمة تنظيف للأعضاء اليتامى، ومعاملة ذرية للانضمام.

إجراء `start` يتحقق من رمز المضيف والجاهزية وتطابق البصمة والإصدار، لكنه يعتمد على snapshot واحد ثم يغير الحالة. يفضل رفض البدء إذا كانت الغرفة ليست `waiting`، واستخدام تحديث مشروط من نوع `UPDATE ... WHERE status = 'waiting'` لتجنب بدءين متزامنين.

## 7. تحليل Socket.IO وNetPlay

المصادقة الأولية للقنوات تتحقق من `roomId` و`memberId` والرمز المجزأ، وتفحص ملكية العضو للغرفة. توجد خريطة للاتصال النشط تمنع الاتصالات المكررة لنفس العضو ونوع العميل. توجد حدود للقطات الحالة ومزامنة متزايدة بالمعرّف.

توجد خرائط snapshots وinput history داخل الذاكرة. هذا مقبول لنشر نسخة واحدة، لكنه لا يضمن الاستمرارية ولا التوافق بين نسخ متعددة. يلزم Redis أو sticky sessions مع مخزن حالة مشترك إذا توسع النشر.

تحتاج أحداث الإدخال والحالة إلى ربط أوضح بجلسة بدأت فعليًا. بعض الأحداث تعتمد على وجود fingerprint في socket data، لكن يجب كذلك التحقق من أن العضو جزء من `pending barrier` الحالي وأن النظام والنسخة تطابق الجلسة النشطة. هذا يقلل احتمال إرسال رسائل من جلسة قديمة إلى قناة الغرفة نفسها.

## 8. تحليل تطبيق الهاتف والمحاكيات

يحتوي التطبيق على مسارات منفصلة لـFamicom وPS1 وPSP وSega وArcade. اختيار الملف يتم عبر Document Picker، وتُفحص الامتدادات والبصمة قبل بدء الجلسة. توجد مكوّنات أصلية Kotlin لنوى Libretro، ويستهدف إعداد Android معمارية `arm64-v8a` فقط.

الاستهداف ARM64 يقلل الحجم ويطابق معظم الهواتف الحديثة، لكنه يستبعد أجهزة Android القديمة أو المحاكيات x86. ينبغي عرض هذا القيد في متطلبات الإصدار أو توفير flavor منفصل إذا كانت تغطية أجهزة أوسع مطلوبة.

تم العثور على أصول قالب React القديمة التالية بلا استخدام runtime مؤكد، وتم حذفها:

- `assets/images/moudie-brand-icon.png`
- `assets/images/partial-react-logo.png`
- `assets/images/react-logo.png`
- `assets/images/react-logo@2x.png`
- `assets/images/react-logo@3x.png`

لم تُحذف `icon.png` و`splash-icon.png` رغم انخفاض استخدامهما runtime، لأنهما ما زالا جزءًا من مسار القالب وأداة تحسين الأيقونات وإعادة التهيئة. حذفهما قد يكسر أمر reset أو إعدادًا مستقبليًا.

## 9. التنظيف المنفذ

تمت إزالة مخرجات `android/app/build` المتتبعة في Git، وعددها **73 ملفًا**، وهي ملفات قابلة لإعادة الإنتاج ولا ينبغي تخزينها في المستودع. تمت إزالة `android/local.properties` لأنه يشير إلى مسار SDK خاص ببيئة تدقيق محلية، وأضيف إلى `.gitignore`. لم تُحذف ملفات النوى الأصلية المطلوبة للبناء، ولا ملفات الهجرات، ولا ملفات الوثائق المرتبطة بالمنتج.

لم يتم تنفيذ حذف واسع للوثائق أو السكربتات. السبب هو أن عدة سكربتات تُستخدم في إعداد Android أو توليد ملفات المحاكيات، بينما بعض الوثائق توثق قرارات التصميم والتراخيص حتى لو لم تكن مستوردة من الكود.

## 10. نتائج الاختبارات والفحوصات

| الفحص | النتيجة |
|---|---|
| `pnpm test` بعد الإصلاح | **48 اختبارًا ناجحًا، و2 متجاوزان** عبر 20 ملف اختبار |
| `pnpm run check` | **ناجح** بعد تصحيح نوع مقعد NetPlay |
| `pnpm run lint` | **ناجح** مع تحذير غير مانع حول `type: module` في `package.json` |
| `pnpm run build` | **ناجح**، وتم إنتاج حزمة خادم بحجم تقريبي 78.4KB |
| `git diff --check` | **ناجح** |
| `pnpm audit --audit-level=high` | **فشل أمنيًا**: 147 ثغرة، 7 منخفضة، 57 متوسطة، 80 عالية، 3 حرجة |
| بناء Android في نسخة التدقيق | **لم يكتمل** لأن إعادة ضبط البيئة أزالت SDK المحلي المشار إليه في `android/local.properties`؛ أزيل المسار المحلي أثناء التنظيف |

الاختباران المتجاوزان مرتبطان ببيئة أو موصلات خارجية، وليسا فشلًا في منطق الغرف. يجب تشغيلهما في CI مزود بمتغيرات البيئة والموصلات المطلوبة قبل الإصدار.

## 11. خطة المعالجة ذات الأولوية

أولًا، يجب تنفيذ حجز المقعد داخل معاملة قاعدة بيانات مع اختبار طلبات متزامنة. هذه المعالجة تمنع تجاوز السعة فعليًا بدل الاكتفاء بفحص منطقي قبل الإدراج.

ثانيًا، يجب تقييد CORS وSocket.IO إلى أصول الإنتاج المعروفة، مع اختبار طلبات preflight وطلبات الاعتماد من الويب والهاتف.

ثالثًا، يجب فصل الاعتماديات التشغيلية عن اعتماديات التطوير، ثم تحديث الثغرات القابلة للتحديث دون تغيير Expo/React Native عشوائيًا. يجب إعادة `pnpm test` و`pnpm run check` و`pnpm run lint` وAndroid Release بعد كل مجموعة تحديث.

رابعًا، يجب إضافة اختبارات تكامل للغرف والردهة، واختبار منافسة الانضمام، واختبار Socket.IO للتحقق من أن العميل لا يستطيع تغيير المقعد الموثق أو إرسال إدخال بعد فقد العضوية.

خامسًا، يجب تعديل `start` ليكون انتقالًا ذريًا من `waiting` إلى `active`، وإضافة تنظيف دوري للخرائط داخل الذاكرة عند إغلاق الغرفة أو انتهاء الجلسة.

## 12. الحكم النهائي

المشروع **سليم وظيفيًا على مستوى اختبارات المصدر الحالية بعد الإصلاحات**، وسياسة السعة الجديدة متسقة عبر العقد المشترك والخادم والواجهات والاختبارات. تم تنظيف آثار بناء وأصول قالب لا تخدم المنتج، واستعيدت الردهة العامة التي كانت معرضة للانحدار.

المشروع **ليس جاهزًا كإصدار إنتاجي نهائي عالي الثقة** قبل معالجة السعة الذرية وCORS وتقرير الاعتماديات. هذه النقاط لا تمنع استخدام نسخة اختبارية محدودة، لكنها تمنع إعلان أن حدود الغرف والحماية الشبكية مضمونة تحت الضغط أو النشر متعدد النسخ.

## المراجع

[1]: https://owasp.org/www-project-application-security-verification-standard/ "OWASP Application Security Verification Standard"

[2]: https://socket.io/docs/v4/middlewares/ "Socket.IO Middleware Documentation"

[3]: https://orm.drizzle.team/docs/transactions "Drizzle ORM Transaction Documentation"

[4]: https://docs.expo.dev/build/introduction/ "Expo Application Build Documentation"

[5]: https://github.com/MahmoudQawry/Moudie-netplay "Moudie NetPlay Source Repository"
